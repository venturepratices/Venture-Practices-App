export default function ClientCredentialsPage() {
  return (
    <div>
      <h2 className="text-lg font-semibold">Credentials</h2>
      <p className="mt-1 text-muted-foreground">Coming in a future phase: encrypted platform credential storage.</p>
    </div>
  );
}
